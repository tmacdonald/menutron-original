<h1>Listing groceries</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groceries.each do |grocery| %>
  <tr>
    <td><%= grocery.how_much + grocery.ingredient_name %></td>
    <td><%= link_to 'Show', [@menu, grocery] %></td>
    <td><%= link_to 'Edit', edit_menu_grocery_path(@menu, grocery) %></td>
    <td><%= link_to 'Destroy', [@menu, grocery], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Grocery', new_menu_grocery_path(@menu) %>
