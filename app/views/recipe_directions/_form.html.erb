<%= form_for([@recipe, @recipe_direction]) do |f| %>
  <% if @recipe_direction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_direction.errors.count, "error") %> prohibited this recipe_direction from being saved:</h2>

      <ul>
      <% @recipe_direction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :order %><br />
    <%= f.text_field :order %>
  </div>
  <div class="field">
    <%= f.label :recipe %><br />
    <%= f.text_field :recipe %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
