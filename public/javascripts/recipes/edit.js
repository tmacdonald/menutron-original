$(document).ready( function() {

  /*
  $.ajax( {
    url: "http://localhost:3000/recipes/minty-mint-mint/directions/new",
    success: function( html ) {
      $(html).appendTo( $("body") );
    }
  });
  */
});
